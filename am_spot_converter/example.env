# Environment variables for API keys
# Copy this file and add your actual API credentials

# Service Configuration
# Options:
#   all     - Use mock services for both Spotify and Apple Music (default)
#   spotify - Use mock service for Spotify only, real API for Apple Music
#   apple   - Use mock service for Apple Music only, real API for Spotify
#   none    - Use real APIs for both services
#
# Legacy values (1/true = all, 0/false = none) are still supported
USE_MOCK_SERVICES=none

# Spotify API credentials (only needed when USE_MOCK_SERVICES=0)
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

# Apple Music API credentials (only needed when USE_MOCK_SERVICES=0)
APPLE_MUSIC_TEAM_ID=your_apple_music_team_id
APPLE_MUSIC_KEY_ID=your_apple_music_key_id
APPLE_MUSIC_PRIVATE_KEY_PATH=your_private_key_path.p8